# 2026-02-13 — Durable summary

## Caffeine skill / MacBook sleep
- User wants MacBook not to sleep when lid is closed and work via Telegram.
- MCP/`mcporter` tool calls for caffeine failed with `unknown tool` errors; `openclaw call` syntax also failed.
- ClawHub CLI `install caffein/caffeine` returned “Skill not found”.
- Manual reinstall done: downloaded from GitHub API `https://api.github.com/repos/openclaw/skills/contents/skills/bunsdev/caffeine` and installed files to `~/.openclaw/skills/caffeine` (SKILL.md, _meta.json). Suggested restarting OpenClaw / re-entering chat and testing.

## Brave API
- Brave Search API key added to config earlier: env.vars.BRAVE_API_KEY set via gateway config.patch; gateway restarted.

## MacBook lid sleep limitation
- User wants MacBook not to sleep when lid is closed.
- **caffeinate -d -i** was started but does NOT prevent sleep when lid is closed — this is a macOS limitation (clamshell mode).
- To work with lid closed: external monitor + power + (optional) external keyboard/mouse required.
- Alternative: leave lid slightly open.
